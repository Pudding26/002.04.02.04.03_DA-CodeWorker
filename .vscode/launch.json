{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "GPU Worker via run.py",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/run.py",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal",
      "justMyCode": false,
      "preLaunchTask": "Start cloudflared tunnel"
    }
  ]
}
